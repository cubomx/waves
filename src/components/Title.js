import React from 'react';
import styles from '../styles/TextStyle.js';
import {Text, View} from 'react-native';

const Title = () => {
    return ( 
        <View>
            <Text styles={{fontFamily: 'Pacifico-Regular'}}>waves</Text>
        </View>
    );

}

export default Title;